在模型权重以 FP16/BF16 加载到 GPU 后，遍历所有线性层（继承 `LinearBase` 的子类），对每个权重矩阵做  **per-channel 对称量化** ，用 INT4/INT8 低精度存储替换原始 FP16 权重，释放显存。推理时逐层**在 HBM 上反量化回计算精度**做矩阵乘法，反量化产生的 FP16/BF16 中间张量是逐层分配、用完即释放的， **不会累积** 。持久驻留的只有压缩后的 INT4/INT8 权重


相比bitsandbytes，bitsandbytes 的 不释放原始 FP16 权重，本方案在模型加载阶段完成量化然后释放原始权重，在推理时不做量化，只做反量化
